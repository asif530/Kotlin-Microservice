# Issues — <Area>

Resolved:

<What broke, in concrete terms (the exact error/symptom). How it was actually caught — this project's convention is "by running it," not "by reading the docs and assuming it would work"; say which. The fix, and why the fix is correct rather than just quieting the symptom. A general lesson, if one generalizes beyond this one instance.>

Unresolved:

<Same shape, but say plainly why it's not fixed yet — missing information, deliberately deferred, blocked on something else. Never silently drop an item here without a resolution; move it to Resolved when it's actually fixed, and update `Archive/Issues/Known-not-fixed` if it's a deliberately-accepted gap rather than a bug.>
