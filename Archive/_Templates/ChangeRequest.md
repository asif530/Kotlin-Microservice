# <Area> — Change Request Request-<NNN>

**Status:** Draft
**Owner role:** <who is asking for this change — project owner / architect / etc.>
**Date:** <YYYY-MM-DD>
**Scope:** <one line: what this request is asking for, and what it explicitly is not>
**Depends on:** <docs this request assumes are already true — e.g. BUSINESS_RULES.md v1.0, ARCHITECTURE.md §N>

---

Req-01:
<Plain description of the situation/problem, as a concrete scenario if possible.>

<Numbered list of what should happen, in the requester's own words. This is the input — it does not need to cite rule IDs or sections; that traceability gets added in the ChangeLog/addendum that resolves this request.>

---

## Resolution tracking

- [ ] `ChangeLog/Request-<NNN>` written (or `Implementation/<Area>/Request-<NNN>` + `ChangeLog/<Area>/Request-<NNN>` for a code change)
- [ ] If this touches a locked document (e.g. `BUSINESS_RULES.md`), the corresponding numbered addendum has been added there — never a silent edit to an existing entry
- [ ] `Archive/Issues/<Area>` updated if anything broke while implementing this
